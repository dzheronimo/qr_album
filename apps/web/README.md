# StoryQR Web Frontend

Production-ready фронтенд для проекта StoryQR, построенный на Next.js 14 с App Router, TypeScript, Tailwind CSS и Radix UI.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ 
- npm или yarn
- Доступ к API Gateway (по умолчанию http://localhost:8080/api/v1)

### Установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd apps/web

# Установите зависимости
npm install

# Скопируйте файл окружения
cp env.example .env.local

# Отредактируйте переменные окружения
nano .env.local
```

### Переменные окружения

Создайте файл `.env.local` на основе `env.example`:

```env
# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_PUBLIC_BASE_URL=https://storyqr.ru
NEXT_PUBLIC_SHORT_BASE_URL=https://sqra.ru

# Feature Flags
NEXT_PUBLIC_ENABLE_PWA=false
NEXT_PUBLIC_SHOW_BETA_BADGE=true

# Development
NODE_ENV=development
```

### Запуск

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен сборки
npm run start

# Линтинг
npm run lint

# Проверка типов
npm run typecheck

# Форматирование кода
npm run format
```

## 🧪 Тестирование

### Unit тесты (Vitest)

```bash
# Запуск всех тестов
npm run test

# Запуск тестов в watch режиме
npm run test -- --watch

# Запуск тестов с покрытием
npm run test -- --coverage
```

### E2E тесты (Playwright)

```bash
# Установка браузеров (первый запуск)
npx playwright install

# Запуск E2E тестов
npm run test:e2e

# Запуск E2E тестов с UI
npm run test:e2e:ui

# Запуск тестов в headless режиме
npm run test:e2e -- --headed
```

## 📁 Структура проекта

```
apps/web/
├── app/                          # Next.js App Router
│   ├── (marketing)/             # Маркетинговые страницы
│   │   ├── page.tsx             # Лендинг
│   │   ├── pricing/page.tsx     # Тарифы
│   │   └── help/page.tsx        # Помощь
│   ├── auth/                    # Аутентификация
│   │   ├── login/page.tsx
│   │   └── register/page.tsx
│   ├── dashboard/               # Личный кабинет
│   │   ├── page.tsx             # Обзор
│   │   ├── albums/              # Управление альбомами
│   │   └── settings/            # Настройки
│   ├── public/                  # Публичные страницы
│   │   └── page/[pageId]/       # Просмотр контента
│   ├── layout.tsx               # Корневой layout
│   └── globals.css              # Глобальные стили
├── components/                   # React компоненты
│   ├── ui/                      # Базовые UI компоненты
│   ├── album/                   # Компоненты альбомов
│   ├── page/                    # Компоненты страниц
│   ├── media/                   # Компоненты медиа
│   ├── qr/                      # Компоненты QR-кодов
│   └── print/                   # Компоненты печати
├── lib/                         # Утилиты и конфигурация
│   ├── api.ts                   # HTTP клиент
│   ├── auth.ts                  # Аутентификация
│   ├── endpoints.ts             # API endpoints
│   └── utils.ts                 # Вспомогательные функции
├── hooks/                       # React хуки
├── types/                       # TypeScript типы
├── tests/                       # Тесты
│   ├── e2e/                     # E2E тесты
│   └── unit/                    # Unit тесты
└── public/                      # Статические файлы
```

## 🎨 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Radix UI** - Headless UI компоненты
- **TanStack Query** - Управление состоянием сервера
- **React Hook Form** - Управление формами
- **Zod** - Валидация схем
- **Framer Motion** - Анимации
- **Lucide React** - Иконки

## 🔧 Основные функции

### Аутентификация
- Регистрация и вход пользователей
- JWT токены с автоматическим обновлением
- Защищенные маршруты
- Управление сессиями

### Управление альбомами
- Создание и редактирование альбомов
- Настройка видимости (публичный/приватный)
- Управление страницами альбома
- Поиск и фильтрация

### Медиа контент
- Загрузка изображений, видео и аудио
- Drag & drop интерфейс
- Предпросмотр файлов
- Управление медиа библиотекой

### QR-коды
- Генерация QR-кодов для страниц
- Скачивание в PNG/SVG форматах
- Короткие ссылки
- Настройка параметров

### Печать
- Создание PDF с QR-кодами
- Настройка размеров наклеек
- Конфигурация сетки печати
- Предпросмотр макета

### Публичный просмотр
- SSR страницы контента
- PIN-защита страниц
- Адаптивный дизайн
- Оптимизация для мобильных

### Аналитика
- Статистика просмотров
- Графики и диаграммы
- Экспорт данных
- KPI метрики

## 🔌 API интеграция

Фронтенд интегрируется с микросервисной архитектурой через API Gateway:

- **Auth Service** - Аутентификация и авторизация
- **Album Service** - Управление альбомами
- **Media Service** - Загрузка и управление медиа
- **QR Service** - Генерация QR-кодов
- **Print Service** - Создание PDF для печати
- **Analytics Service** - Сбор и анализ метрик
- **Billing Service** - Управление подписками

## 🎯 Производительность

- **Lighthouse Score**: >90 по всем метрикам
- **LCP**: <1.8s (P75)
- **JS Bundle**: <150KB gzip
- **Image Optimization**: Next.js Image компонент
- **Code Splitting**: Автоматическое разделение кода
- **Caching**: TanStack Query кэширование

## 🔒 Безопасность

- **XSS Protection**: Санитизация пользовательского ввода
- **CSRF Protection**: Токены для форм
- **Secure Headers**: Настроенные HTTP заголовки
- **Content Security Policy**: Ограничение источников
- **JWT Security**: Безопасное хранение токенов

## 📱 PWA поддержка

PWA функциональность доступна через feature flag:

```env
NEXT_PUBLIC_ENABLE_PWA=true
```

Включает:
- Service Worker
- Offline поддержка
- App manifest
- Push уведомления

## 🌐 Интернационализация

Поддержка русского и английского языков:

```typescript
// Пример использования
const t = useTranslations('common');
return <h1>{t('welcome')}</h1>;
```

## 🚀 Деплой

### Vercel (рекомендуется)

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel --prod
```

### Docker

```bash
# Сборка образа
docker build -t storyqr-web .

# Запуск контейнера
docker run -p 3000:3000 storyqr-web
```

### Nginx

Пример конфигурации для продакшена:

```nginx
server {
    listen 80;
    server_name storyqr.ru;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 🐛 Отладка

### Логирование

```typescript
// Включение debug логов
localStorage.setItem('debug', 'storyqr:*');
```

### React DevTools

Установите расширение для браузера для отладки React компонентов.

### TanStack Query DevTools

Включены в development режиме для отладки запросов.

## 📚 Дополнительные ресурсы

- [Next.js документация](https://nextjs.org/docs)
- [Tailwind CSS документация](https://tailwindcss.com/docs)
- [Radix UI документация](https://www.radix-ui.com/docs)
- [TanStack Query документация](https://tanstack.com/query/latest)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature ветку
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.
