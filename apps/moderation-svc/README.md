# Moderation Service

Сервис для модерации контента с AI интеграцией и настраиваемыми правилами.

## 🚀 Возможности

- **AI Модерация**: Автоматический анализ контента с помощью AI
- **Правила модерации**: Настраиваемые правила для автоматической модерации
- **Ручная модерация**: Интерфейс для модераторов
- **Гибридная модерация**: Комбинация AI, правил и ручной проверки
- **Журнал модерации**: Детальная история всех действий
- **Статистика**: Аналитика и отчеты по модерации

## 📋 Типы контента

- **Image**: Изображения и фотографии
- **Text**: Текстовый контент
- **Video**: Видео файлы
- **Audio**: Аудио файлы
- **URL**: Веб-ссылки
- **File**: Произвольные файлы

## 🛠️ Установка и запуск

### Требования

- Python 3.11+
- PostgreSQL 13+
- Redis (для будущей интеграции)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE moderation_db;
```

2. Скопируйте файл конфигурации:
```bash
cp env.example .env
```

3. Отредактируйте `.env` файл с вашими настройками

4. Запустите миграции:
```bash
alembic upgrade head
```

### Запуск сервиса

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8010 --reload
```

## 📚 API Документация

После запуска сервиса документация доступна по адресу:
- Swagger UI: http://localhost:8010/docs
- ReDoc: http://localhost:8010/redoc

## 🔧 Основные эндпоинты

### Модерация
- `POST /api/v1/moderation/requests` - Создание запроса на модерацию
- `GET /api/v1/moderation/requests` - Получение запросов пользователя
- `GET /api/v1/moderation/requests/{id}` - Получение запроса по ID
- `POST /api/v1/moderation/requests/{id}/process` - Обработка запроса
- `GET /api/v1/moderation/pending` - Получение ожидающих запросов
- `GET /api/v1/moderation/stats` - Статистика модерации

### Правила
- `POST /api/v1/rules` - Создание правила модерации
- `GET /api/v1/rules` - Получение списка правил
- `GET /api/v1/rules/active` - Получение активных правил
- `GET /api/v1/rules/{id}` - Получение правила по ID
- `PUT /api/v1/rules/{id}` - Обновление правила
- `DELETE /api/v1/rules/{id}` - Удаление правила
- `POST /api/v1/rules/{id}/toggle` - Переключение статуса

### Журнал
- `GET /api/v1/logs/request/{id}` - Записи для запроса
- `GET /api/v1/logs/actor/{type}` - Записи по исполнителю
- `GET /api/v1/logs/action/{action}` - Записи по действию
- `GET /api/v1/logs/recent` - Недавние записи
- `GET /api/v1/logs/search` - Поиск записей
- `GET /api/v1/logs/stats` - Статистика журнала
- `GET /api/v1/logs/export` - Экспорт записей

### AI
- `GET /api/v1/ai/stats` - Статистика AI модерации

## 🗄️ Модели данных

### ModerationRequest
- `id`: Уникальный идентификатор
- `user_id`: ID пользователя
- `content_type`: Тип контента
- `content_id`: ID контента
- `content_url`: URL контента
- `content_text`: Текстовое содержимое
- `moderation_type`: Тип модерации
- `status`: Статус модерации
- `priority`: Приоритет

### ModerationResult
- `id`: Уникальный идентификатор
- `request_id`: ID запроса
- `is_approved`: Одобрен ли контент
- `confidence_score`: Уровень уверенности
- `risk_score`: Уровень риска
- `violations`: Нарушения
- `ai_model`: Использованная AI модель
- `moderator_id`: ID модератора

### ModerationRule
- `id`: Уникальный идентификатор
- `name`: Название правила
- `content_type`: Тип контента
- `conditions`: Условия правила
- `action`: Действие при срабатывании
- `is_active`: Активно ли правило
- `priority`: Приоритет правила

### ModerationLog
- `id`: Уникальный идентификатор
- `request_id`: ID запроса
- `action`: Действие
- `actor_type`: Тип исполнителя
- `actor_id`: ID исполнителя
- `message`: Сообщение
- `created_at`: Время создания

## 🔄 Статусы модерации

- `pending`: Ожидает обработки
- `approved`: Одобрено
- `rejected`: Отклонено
- `flagged`: Помечено для проверки
- `under_review`: На рассмотрении
- `auto_approved`: Автоматически одобрено
- `auto_rejected`: Автоматически отклонено

## ⚡ Типы модерации

- `automatic`: Автоматическая (на основе правил)
- `manual`: Ручная (модератором)
- `ai`: AI анализ
- `hybrid`: Гибридная (AI + правила + ручная)

## 🧪 Тестирование

```bash
# Запуск тестов
pytest

# Запуск с покрытием
pytest --cov=app
```

## 📊 Мониторинг

### Health Check
- `GET /api/v1/health` - Проверка состояния сервиса
- `GET /api/v1/health/db` - Проверка состояния базы данных

### Статистика
- `GET /api/v1/moderation/stats` - Статистика модерации
- `GET /api/v1/rules/stats` - Статистика правил
- `GET /api/v1/logs/stats` - Статистика журнала
- `GET /api/v1/ai/stats` - Статистика AI

## 🔮 Планы развития

- [ ] Интеграция с реальными AI сервисами (OpenAI, Google AI)
- [ ] Машинное обучение на основе истории модерации
- [ ] Веб-интерфейс для модераторов
- [ ] Уведомления в реальном времени
- [ ] A/B тестирование правил модерации
- [ ] Интеграция с внешними API модерации
- [ ] Система репутации пользователей
- [ ] Автоматическое обучение правил

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

MIT License
