# Common
PUBLIC_BASE_URL=http://localhost:8080
JWT_SECRET=change_me_dev
JWT_EXPIRE_MINUTES=15
REFRESH_EXPIRE_DAYS=7

# Redis/Rabbit
REDIS_URL=redis://redis:6379/0
RABBITMQ_URL=amqp://guest:guest@rabbit:5672//

# Postgres single instance with multiple DBs
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=qr
POSTGRES_PASSWORD=secret

# Databases per service (created via init SQL)
DB_AUTH=authdb
DB_PROFILE=profiledb
DB_ALBUM=albumdb
DB_MEDIA=mediadb
DB_QR=qrdb
DB_ANALYTICS=analyticsdb
DB_BILLING=billingdb

# S3/MinIO
S3_ENDPOINT=http://minio:9000
S3_REGION=us-east-1
S3_BUCKET=qr-media
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123

# Scan Gateway
SCAN_GATEWAY_BASE=http://scan-gateway:8000
RATE_LIMIT_MINUTE=60

# Notification (email demo)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM=noreply@qr.local
TELEGRAM_BOT_TOKEN=your_telegram_token_here
