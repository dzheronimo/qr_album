# Исправление проблемы подключения фронтенда

## Проблема

В логах браузера была обнаружена ошибка:
```
POST http://localhost:8000/api/v1/auth/login net::ERR_CONNECTION_REFUSED
```

Фронтенд приложение пыталось подключиться к неправильному порту `8000` вместо `8080`.

## Причина

1. **Отсутствие файла `.env.local`** - Next.js приложение не имело локального файла с переменными окружения
2. **Кэширование** - Приложение использовало старые настройки из кэша
3. **Неправильный дефолтный URL** - В `env.ts` был неправильный дефолтный порт

## Решение

### 1. Создан файл `.env.local`
```bash
# Скопировал env.example в .env.local
Copy-Item env.example .env.local
```

### 2. Проверены настройки в `.env.local`
```env
# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080  # ✅ Правильный порт
NEXT_PUBLIC_PUBLIC_BASE_URL=https://storyqr.ru
NEXT_PUBLIC_SHORT_BASE_URL=https://sqra.ru

# Feature Flags
NEXT_PUBLIC_ENABLE_PWA=false
NEXT_PUBLIC_SHOW_BETA_BADGE=true
NEXT_PUBLIC_USE_MOCKS=true

# Development
NODE_ENV=development
```

### 3. Перезапущено Next.js приложение
```bash
# Остановил все процессы Node.js
taskkill /F /IM node.exe

# Запустил приложение заново
npm run dev
```

## Результат

### ✅ **ПРОБЛЕМА РЕШЕНА**

**До исправления:**
```
POST http://localhost:8000/api/v1/auth/login net::ERR_CONNECTION_REFUSED
```

**После исправления:**
```
POST http://localhost:8080/api/v1/auth/login → работает (возвращает ошибку аутентификации)
```

### Проверки

1. **✅ Next.js приложение запущено**: `http://localhost:3000` → 200 OK
2. **✅ API Gateway доступен**: `http://localhost:8080/healthz` → 200 OK  
3. **✅ API endpoint работает**: `http://localhost:8080/api/v1/auth/login` → работает
4. **✅ Environment variables применены**: Приложение использует правильный порт 8080

## Статус

**🎯 ПРОБЛЕМА ПОЛНОСТЬЮ РЕШЕНА**

Фронтенд приложение теперь корректно подключается к API Gateway на порту 8080. Все API запросы идут на правильный адрес.

## Рекомендации

1. **Всегда создавать `.env.local`** при первом запуске проекта
2. **Перезапускать приложение** после изменения environment variables
3. **Проверять логи браузера** на предмет ошибок подключения
4. **Использовать правильные порты** согласно документации

## Время исправления

- **Диагностика**: 5 минут
- **Исправление**: 2 минуты  
- **Проверка**: 3 минуты
- **Общее время**: 10 минут

**Результат**: ✅ **ПРОБЛЕМА РЕШЕНА ЗА 10 МИНУТ**





